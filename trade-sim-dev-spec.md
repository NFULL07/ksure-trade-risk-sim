# 무역 리스크 대응 시뮬레이션 — 개발 명세서 (Codex 작업용)

> 이 문서는 AI 코딩 도구(Codex)에게 전달하기 위한 개발 지침서다.
> 아래 명세를 그대로 구현하면 작동하는 데모가 나오도록 작성했다.
> 〔교체 필요〕 표시가 붙은 값은 실제 공공데이터포털 값으로 나중에 교체할 자리다. 그 외 숫자는 게임 밸런스용 확정값이다.

---

## 0. 한 줄 요약

가상 수출기업 사장이 되어, 한국무역보험공사(무보공)의 위험 데이터를 읽고 어느 시장에 어떤 조건으로 거래할지 판단해 자금을 지키는 웹 기반 교육 시뮬레이션. 단일 HTML 파일로 구현한다.

---

## 1. 프로젝트 목적과 성격

- **목적**: 무보공 공공데이터(국가·업종별 위험지수, 신용위험지수)의 쓸모를 "직접 판단하는 경험"으로 체감시킨다.
- **교육 메시지 (핵심)**: "위험한 시장을 피하라"가 아니라 **"위험은 있되, 데이터로 읽고 결제조건·보험으로 관리하면 감당할 수 있다"**.
- **성격**: 대국민/교육용 데모. 완성된 상용 게임이 아니라, 발표 시연이 가능한 프로토타입.

---

## 2. 절대 원칙과 금지사항 (반드시 준수)

1. **미래 예측 도구가 아니다.** UI·문구 어디에도 "미래를 예측한다", "다음 분기 시장을 맞힌다" 같은 표현을 쓰지 않는다. 이 게임은 *주어진 위험 정보에 얼마나 잘 대응하는가*를 다룬다. 각 턴의 위험 정보는 "현재 알려진 위험지수"로 제시한다.
2. **실존 기업을 위험하다고 단정하지 않는다.** 게임에 등장하는 바이어·기업은 모두 가상이다. 실제 회사명·실제 개별기업 데이터를 쓰지 않는다.
3. **데이터 출처를 표시한다.** 위험지수 개념을 소개하는 화면에 "한국무역보험공사 공공데이터(국가·업종별 위험지수, 신용위험지수) 기반" 및 "게임 내 수치 일부는 교육용 예시"임을 명시한다.
4. **면책 문구.** 시작 또는 종료 화면에 "본 시뮬레이션은 교육용이며, 실제 거래 판단의 근거가 아니다"를 넣는다.
5. **개인정보·로그인 없음.** 저장 기능은 브라우저 메모리(변수) 내에서만. localStorage 등 외부 저장 사용 금지.

---

## 3. 게임 구조

### 3.1 전체 흐름

```
시작 화면 (규칙·데이터 소개)
  → 스테이지 1 (2턴): 위험지수 읽기
  → 스테이지 2 (4턴): 조건으로 위험 관리
  → 결과 화면 (자금 결산 + 교훈 요약)
```

- 발표에서 지표 읽기 설명이 반복되지 않도록 스테이지 1은 2턴으로 압축한다.
- 결제조건·보험·특수사례를 보여주는 스테이지 2는 판단 경험을 위해 4턴을 유지한다.

### 3.2 한 턴의 사이클

1. **이번 분기 시장 후보 3개 제시** — 각 후보는 (국가 × 업종), 위험등급(RI), 신용위험지수, 예상 수익률을 함께 보여준다.
2. **플레이어 선택**
   - 스테이지 1: 시장 하나 선택 (결제조건은 "후불"로 고정)
   - 스테이지 2: 시장 선택 + 결제조건(선불/부분선불/후불) 선택 + 보험 가입 여부
3. **판정** — 아래 4장 공식으로 회수 성공 / 미회수 사고를 결정.
4. **자금 반영** — 성공 시 수익 가산, 사고 시 손실 차감.
5. **피드백 문구** — "왜 이 결과가 나왔는지"를 데이터와 연결해 한 줄로 설명.
6. 다음 턴으로. 자금이 파산 임계선 아래로 떨어지면 즉시 게임 오버.

### 3.3 승패 조건

- **시작 자금**: 10,000 크레딧
- **파산 임계선**: 3,000 크레딧 미만 → 실패(게임 오버)
- **스테이지 2 목표**: 스테이지 2 시작 시점의 자금보다 **400 크레딧 이상 증가**
- **최종 판정**: 파산선 미만이면 "실패", 파산을 피하고 스테이지 2 목표를 달성하면 "성공", 파산은 피했지만 목표에 못 미치면 "생존"
- 성공을 절대 자금 10,000으로 판정하지 않는다. 선택지가 없는 스테이지 1의 운이 최종 승패를 미리 결정하지 않도록 스테이지 2의 상대 성과로 판정한다.

---

## 4. 판정 로직 (혼합형)

### 4.1 기본 개념

이 게임은 한국무역보험공사가 **이미 개방한 세 가지 공공데이터**를 각각 다른 역할로 활용한다. 세 지표는 서로 겹치는 것처럼 보이지만 답하는 질문이 다르므로, 카드에 세 지표를 모두 표시하되 각 지표에 역할 라벨을 붙인다.

| 지표(개방데이터) | 카드 라벨 | 답하는 질문 | 게임 내 역할 |
|---|---|---|---|
| 국가별 업종별 위험지수 (RI1~RI5) | 종합 위험 | 이 시장 전반이 얼마나 위험한가 | 기본 사고확률 |
| 주요 업종별 신용위험지수 (R급 비율) | 업종 신용위험 | 이 업종의 대금 미회수 위험은 어떤가 | 사고확률 가중 |
| 국가신용등급 (1~7) | 나라 위험 | 전쟁·송금제한 등 국가 차원 위험 | 별도 비상위험 이벤트 발동 |

> 세 지표는 모두 실제 개방된 데이터다. 미공개 데이터(국외기업 경영분석 등)는 신뢰성을 위해 이 게임의 변수로 쓰지 않는다.

각 시장은 위험등급(RI)에 따라 **기본 사고확률**과 **수익률**을 가진다. 위험이 높을수록 사고확률도, 수익률도 높다(트레이드오프). 여기에 신용위험지수가 사고확률을 추가로 가중하고, 국가신용등급은 별도의 비상위험 이벤트로 작동한다. 플레이어의 결제조건·보험 선택이 이 확률과 손실을 **보정**한다.

### 4.2 시장별 기본값 (게임 밸런스용 확정값)

| 위험등급 | 기본 사고확률 | 수익률(성공 시) |
|---|---|---|
| RI1 | 5% | +8% |
| RI2 | 15% | +12% |
| RI3 | 30% | +20% |
| RI4 | 50% | +32% |
| RI5 | 70% | +45% |

- 1회 거래 규모(거래액): **3,000 크레딧** (고정)
- 성공 시 획득 = 거래액 × 수익률 (예: RI3 성공 = 3,000 × 0.20 = +600)

### 4.2.1 신용위험지수 가중 (업종 신용위험)

이 게임에 사용한 신용위험지수(R급 비율)는 **업종 단위**이며 RI 기반 기본 사고확률에 가중치로 더해진다. 같은 업종은 국가가 달라도 같은 라벨을 사용한다. 국가별 신용위험지수는 별도 개방 데이터지만 이 3개 파일 조인에는 포함하지 않는다. 연속값은 3단계로 단순화한다.

| 신용위험지수 라벨 | 사고확률 가중 |
|---|---|
| 낮음 | +0%p |
| 중간 | +5%p |
| 높음 | +12%p |

- 예: RI3(기본 30%) 시장의 신용위험지수가 "높음"이면 → 사고확률 42%.
- 이렇게 하면 "종합 위험(RI)은 같아도 신용위험이 다르면 결과가 갈린다"는 판단 재미가 생긴다.

### 4.2.2 국가신용등급 → 비상위험 이벤트 (나라 위험)

국가신용등급(1~7)은 사고확률에 직접 넣지 않고, **별도의 비상위험 이벤트** 발동 확률로 작동한다. 비상위험은 거래 상대의 신용과 무관하게 국가 사유(전쟁·내란·송금 제한 등)로 대금이 막히는 위험이다.

| 국가신용등급 | 비상위험 이벤트 발동 확률 |
|---|---|
| 1~2 (안전) | 0% |
| 3~4 (보통) | 8% |
| 5 (주의) | 20% |
| 6~7 (위험) | 35% |

- 이벤트 발동 시: 결제조건·보험과 무관하게 해당 턴 거래가 "송금 지연/거래 중단" 처리되어 거래액의 일부(예: 50%, -1,500)가 묶이거나 손실된다.
- 단, **무역보험(비상위험 담보) 가입 시** 이 손실의 60%를 보상한다 → "비상위험은 결제조건이 아니라 보험으로 대비하는 것"이라는 교육 포인트.
- 연출 예: "OO국에 송금 제한 조치가 내려졌습니다. 대금 회수가 지연됩니다." + 그 나라의 국가신용등급을 함께 표시.

### 4.3 결제조건 보정 (스테이지 2)

| 결제조건 | 사고확률 보정 | 수익률 보정 | 사고 시 손실 |
|---|---|---|---|
| 후불(전액) | ×1.0 (그대로) | ×1.35 | 거래액 전액 손실 (-3,000) |
| 부분선불(50%) | ×0.6 | ×0.85 | 거래액 절반 손실 (-1,500) |
| 선불(전액) | ×0.5 | ×0.55 | 생산·선적 비용 노출 (-1,200) |

> 의미: 선불로 갈수록 안전하지만 수익률이 크게 낮아진다. 전액 선불이어도 이미 투입된 생산·선적 비용의 노출을 1,200으로 남겨 무손실 지배전략이 생기지 않게 한다. 반대로 후불은 위험하지만 수익이 가장 크다. "안전 vs 수익"의 긴장을 만든다.
>
> **이 수치는 정책별 3만 회 몬테카를로 시뮬레이션으로 재검증했다.** 결과와 실행 조건은 부록 A 및 `balance-report.md`에 기록한다.

### 4.4 보험 보정 (스테이지 2)

- **보험 가입 시**: 성공 예상수익(거래액 × RI 수익률 × 결제조건 수익 보정)의 **35%**를 10 크레딧 단위로 반올림하되, 최소 50·최대 250 크레딧으로 제한한다. 일반 사고 발생 시 손실의 **70%**를 보상한다.
- **비상위험 이벤트**(4.2.2)에도 적용: 손실의 **60%** 보상. 비상위험은 결제조건으로 못 막으므로 보험이 유일한 대비책.
- 보험료는 성공·일반 사고·비상위험의 모든 결과 경로에서 납부한다.
- 단, **사기성 사고(4.6 해킹 사기 이벤트)는 보험 보상 대상이 아님.**

> 정액 보험료 300은 저위험·선불 조합에서 거래 성공 후에도 순손실을 만들었다. 수익 연동 보험료는 모든 성공 조합의 순이익을 양수로 유지하면서도 보험의 비용을 남긴다.

### 4.5 판정 계산 순서 (구현 의사코드)

```
function resolveTurn(market, paymentTerm, hasInsurance):
    dealAmount = 3000
    grossProfit = dealAmount × market.returnRate × paymentTermReturnMult
    premium = roundTo10(clamp(grossProfit × 0.35, 50, 250))

    # (A) 비상위험 이벤트 먼저 판정 (국가신용등급 기반, 4.2.2)
    if random(0~1) < market.emergencyProb:      # 국가등급별 발동확률
        loss = 1500                              # 거래액 일부 묶임/손실
        if hasInsurance: loss = loss × 0.4       # 비상위험 담보 60% 보상
        if hasInsurance: loss += premium          # 모든 결과 경로에서 보험료 납부
        fund -= loss
        return "EMERGENCY"                        # 이 턴은 비상위험으로 종료

    # (B) 일반 사고 판정 (RI + 신용위험지수, 4.2 / 4.2.1)
    baseProb = market.accidentProb               # RI 기반 (4.2 표)
    prob = (baseProb + market.creditWeight) × paymentTermProbMult   # 신용위험 가중(4.2.1) + 결제조건(4.3)
    roll = random(0~1)

    if roll >= prob:   # 회수 성공
        profit = grossProfit
        if hasInsurance: profit -= premium
        fund += profit
        result = "SUCCESS"
    else:              # 미회수 사고
        loss = paymentTermLoss                   # 4.3 표
        if hasInsurance: loss = loss × 0.3 + premium # 70% 보상 + 보험료
        fund -= loss
        result = "ACCIDENT"

    return result
```

### 4.6 특수 이벤트 — 무역사기 확인 절차 (교육 포인트)

- 스테이지 2의 2턴과 마지막 턴을 특수 이벤트 확인 지점으로 두며, 일반 모드에서는 사례별 **8%** 확률로 발생시킨다. 발표용 재현 모드에서는 두 사례를 모두 노출한다.
- 사례 1 — 위조 송금증: 바이어가 송금 확인증을 보내며 입금 반영 전 선적을 요구한다. → [확인증만 믿고 선적 / 은행 입금 내역 확인]
- 사례 2 — 계좌 변경 메일: 거래처를 사칭한 메일이 새 계좌 송금을 요구한다. → [새 계좌로 바로 송금 / 기존 번호로 유선 확인]
- 위험한 선택을 하면 제3자 편취 피해가 발생한다. **이 손실은 보험에 가입했어도 보상되지 않는다.**
- 안전한 선택을 하면 피해 예방 결과를 먼저 보여주고, 사용자가 확인한 뒤 원래 거래 판정으로 이어간다. 결과 팝업 없이 즉시 다음 단계로 넘어가서는 안 된다.
- 이 이벤트의 목적은 "데이터로 시장 위험은 관리해도, 문서·연락처·실제 입금은 별도의 경로로 확인해야 한다"를 각인시키는 것이다.

---

## 5. 데이터 명세

### 5.1 데이터의 두 종류

- **종류 A (실제 개방 데이터)**: 국가·업종별 RI등급, 신용위험지수, 국가신용등급. 포털 파일을 받아 JSON 시장 리스트로 심는다(5.2). 이게 "무보공 데이터 기반"이라는 명분의 근거다. 채우기 전에는 `isSampleData: true`로 두고 화면에 "예시 데이터" 문구를 노출한다.
- **종류 B (게임 밸런스 확정값)**: 사고확률·수익률·신용가중·비상위험확률·자금·보정계수 등. 공공데이터에 없는, 설계자가 정하는 값. 코드 config 상수로 두며 교체 불필요. 종류 A(원천)와 종류 B(설계)는 반드시 분리해 관리한다.

### 5.2 데이터 소스 방식 — 실제 파일을 JSON으로 심어 랜덤 추출

시장을 6개로 고정하지 않는다. 무보공이 개방한 세 데이터 파일을 받아 **하나의 시장 리스트(JSON)** 로 합쳐 게임 코드에 심고, 매 턴 그 리스트에서 **무작위로 3개 시장을 뽑는다.** 이렇게 하면 실제 개방 데이터를 그대로 활용하면서 재현성·다양성이 확보된다.

#### 5.2.1 데이터 준비 절차 (개발 전, 사람이 수행)

1. 공공데이터포털(data.go.kr)에서 세 파일을 받는다.
   - "한국무역보험공사_국가별 업종별 위험지수" (RI등급)
   - "한국무역보험공사_주요 업종별 신용위험지수" (R급 비율)
   - "한국무역보험공사_국가신용등급" (1~7)
2. RI와 국가등급은 국가명으로, 신용위험은 명시적인 업종 교차표로 조인한다. **세 값이 모두 존재하는 조합만** 남긴다(없는 값은 추측으로 채우지 않는다). 신용위험은 업종 단위이므로 국가가 달라도 같은 업종에는 같은 값이 반복된다. 이때 RI 기준 세 층(낮음 RI1~2 / 중간 RI3 / 높음 RI4~5)이 **각각 최소 2~3개 이상** 채워지도록 확인한다.
3. 신용위험지수(연속 숫자)는 낮음/중간/높음 3구간으로 변환한다(구간 기준은 사람이 정함).
4. 결과를 아래 스키마의 JSON 배열로 만들어 게임 코드에 넣는다.

#### 5.2.2 시장 리스트 JSON 스키마 (플랫 구조, AI-Ready 지향)

```json
{
  "source": "한국무역보험공사 공공데이터포털 (위험지수/신용위험지수/국가신용등급)",
  "isSampleData": false,
  "dataVintages": {
    "riskIndex": "2025-04",
    "sectorCreditRisk": "2023",
    "countryGrade": "2026-02-06",
    "countryGradeMethod": "국가별 최신 평가일"
  },
  "creditRiskScope": "업종 단위",
  "markets": [
    {
      "id": "M001",
      "countryCode": "VNM",      // ISO 3166 (교체: 실제값)
      "country": "베트남",         // (교체: 실제값)
      "sector": "가구",            // (교체: 실제값)
      "ri": 2,                    // RI1~5 (교체: 실제값)
      "creditRisk": "낮음",        // 낮음/중간/높음 (교체: 신용위험지수 변환)
      "countryGrade": 3           // 국가신용등급 1~7 (교체: 실제값)
    }
  ]
}
```

- `isSampleData`: 실제값으로 다 채우기 전까지는 `true`로 두고, 화면에 "예시 데이터" 문구를 노출한다. 실제 포털값으로 채우면 `false`.
- 파생값(사고확률·수익률·신용가중·비상위험확률)은 JSON에 넣지 않는다. 게임 코드가 `ri`·`creditRisk`·`countryGrade`를 4장 표에 대입해 **런타임에 계산**한다. 그래야 실제 데이터(원천)와 게임 밸런스(설계)가 분리된다.

#### 5.2.3 층화 추출 로직 (의사코드)

매 턴 후보는 **3개**, 단 위험도가 겹치지 않도록 **RI등급 기준 3개 층에서 하나씩** 뽑는다. 그래야 매 턴 "안전·중간·위험"을 저울질하는 경영 판단이 성립한다.

- 낮음층: RI1~RI2
- 중간층: RI3
- 높음층: RI4~RI5

```
function drawMarketsForTurn(allMarkets):
    low  = filter(allMarkets, m => m.ri <= 2)     # 낮음층
    mid  = filter(allMarkets, m => m.ri == 3)     # 중간층
    high = filter(allMarkets, m => m.ri >= 4)     # 높음층

    picks = [ pickRandom(low), pickRandom(mid), pickRandom(high) ]
    return shuffle(picks)     # 카드 순서는 섞어서 노출(층 순서 고정 방지)

# 각 시장의 파생값은 뽑은 뒤 4장 표로 계산
function deriveStats(market):
    market.accidentProb   = RI_TABLE[market.ri].prob          # 4.2
    market.returnRate     = RI_TABLE[market.ri].return        # 4.2
    market.creditWeight   = CREDIT_TABLE[market.creditRisk]   # 4.2.1
    market.emergencyProb  = GRADE_TABLE[market.countryGrade]  # 4.2.2
    return market
```

- **각 층에 최소 1개 이상**의 시장이 있어야 한다. 시장 리스트 구성 시 세 층이 모두 채워지도록 국가·업종을 선정한다(예: 층당 2~3개 이상 권장).
- 특정 층이 비면 그 턴은 남은 층에서 대체하되, 가능하면 데이터 준비 단계에서 세 층을 고르게 확보해 이 상황을 피한다.
- 카드 노출 순서는 매번 섞어, "왼쪽이 항상 안전"처럼 층 순서로 답을 유추하지 못하게 한다.

### 5.3 실제 확인된 사실 (이건 그대로 사용 가능, 교체 불필요)

- 위험지수(K-SURE Risk Index)의 위험등급은 **RI1~RI5** 다섯 단계이며, 숫자가 클수록 주의가 필요한 거래다. (카드 라벨: 종합 위험)
- 이 게임에서 쓰는 주요 업종별 신용위험지수는 공사가 신용등급을 평가한 국외기업 중 신용불량(R급) 비율을 업종 단위로 집계한 값이며, 높을수록 대금 미회수 위험이 크다. (카드 라벨: 업종 신용위험)
- 국가신용등급은 1~7등급이며, **숫자가 작을수록 안전하고 클수록 위험**하다. 전쟁·내란·송금 제한 같은 비상위험을 반영한다. (카드 라벨: 나라 위험 → 비상위험 이벤트)
- 위 정의는 데이터 소개 화면의 설명 문구로 사용해도 된다.

---

## 6. 화면 흐름과 UX

### 6.1 화면 목록

1. **시작 화면**: 제목 / 게임 목적 한 줄 / "무보공 공공데이터 기반" 및 예시데이터 안내 / 면책 문구 / [시작] 버튼.
2. **데이터 소개(튜토리얼) 화면**: 세 지표를 각 라벨과 함께 2~3문장으로 소개 — 위험지수(종합 위험), 주요 업종별 신용위험지수(업종 신용위험), 국가신용등급(나라 위험). 신용위험이 업종 단위라는 한계와 세 지표의 기준시점(RI 2025-04 / 신용위험 2023 / 국가등급 국가별 최신 평가일, 전체 최신 2026-02-06)을 함께 표시한다.
3. **턴 화면 (메인)**:
   - 상단: 현재 분기(턴) / 보유 자금 / 목표선 표시
   - 중앙: 시장 후보 3개 카드. 각 카드에 세 지표를 **역할 라벨과 함께** 표시 — 「종합 위험 RI4 / 업종 신용위험 높음 / 나라 위험 5등급」 + 예상 수익률. 스테이지 1의 일반 사고확률은 판정 전에는 숨기고 결과 화면에서 공개한다.
   - 스테이지 2: 결제조건 선택 UI + 보험 체크박스 (보험은 비상위험도 담보함을 안내)
   - 하단: [이 거래 진행] 버튼
4. **결과 팝업(턴마다)**: 성공/사고/비상위험 여부 + 자금 변동 + "왜"에 대한 한 줄 데이터 피드백 (예: "국가신용등급 5등급 시장에서 송금 제한이 발생했습니다").
5. **종료 화면**: 최종 자금 / 성공·생존·실패 판정 / 이번 플레이의 교훈 요약 (예: "종합 위험만 보지 말고, 나라 위험은 보험으로 대비해야 한다") / [다시하기].

### 6.2 톤·비주얼 가이드

#### 6.2.1 원칙

- 깔끔하고 신뢰감 있는 톤(교육·공공 성격). 과한 게임 이펙트 자제.
- 게임 그래픽에 생성형 비트맵을 사용하지 않는다. 모든 그래픽은 인라인 SVG·CSS·이모지로 구현한다. 단, 시작 화면의 사용자가 제공한 **K-SURE 공식 로고 PNG 1개**는 명시적 예외로 base64 내장한다. 게시 주체가 로고 사용 권한과 공식 제작물 오인 가능성을 확인한다.
- 색만으로 정보를 전달하지 않는다. 색·숫자·라벨을 항상 병기(색각 이상 고려).
- 모바일 반응형. 카드가 세로로 쌓이게.

#### 6.2.2 그래픽 요소 (구현 우선순위 순)

**[필수] 위험도 게이지 — SVG 또는 CSS**
RI1~RI5를 5칸 세그먼트 바 또는 반원 계기판으로 표현. 녹색(RI1) → 황색(RI3) → 적색(RI5) 그라데이션. 게이지 옆에 "RI4 / 종합 위험" 텍스트 병기.

**[필수] 신용위험 표시 — CSS**
낮음/중간/높음을 점 3개(●○○ / ●●○ / ●●●)로. 텍스트 라벨 "업종 신용위험: 높음" 병기.

**[필수] 국가신용등급 표시 — CSS**
1~7 등급을 7칸 스텝 바로. 해당 등급 칸 강조. "나라 위험: 5등급" 병기.

**[필수] 국기 — 이모지**
시장 카드 상단에 해당 국가 국기 이모지(🇻🇳 🇲🇽 🇩🇪 등). 텍스트라 용량 0, 국가 식별이 즉각적. JSON의 `countryCode`(ISO 3166)에서 이모지로 변환하는 함수를 둔다.

**[필수] 자금 추이 차트 — 인라인 SVG**
턴이 진행될수록 자금 변화를 라인/막대로 그림. 시작자금 기준선과 파산 임계선(3,000)을 점선으로 표시. 경영 시뮬 느낌을 가장 크게 살리는 요소.

**[권장] 결과 연출 아이콘 — 인라인 SVG**
성공(컨테이너/체크), 사고(경고 삼각형), 비상위험(국가·차단 아이콘), 보험 적용(방패) 등 단순 라인 아이콘. 각 20~24px, 단색.

**[선택/확장] 세계지도 — SVG**
시장 국가의 대략적 위치를 표시. 좌표 데이터가 필요해 손이 가므로, 시간이 남을 때만 붙인다. 없어도 게임 성립.

#### 6.2.3 레이아웃·타이포

- 보유 자금은 화면에서 가장 큰 숫자로(사장의 관심사). 지표는 그보다 작게.
- 시장 카드는 3개가 가로로 나란히(모바일에선 세로 스택). 카드 안 위계: 국기+국가·업종 → 세 지표 → 예상 수익률 → 선택 버튼.
- 애니메이션은 절제: 자금 변동 시 숫자 카운트업, 게이지 채워지는 정도로 충분.

---

## 7. 기술 지침

- **단일 HTML 파일**로 구현 (HTML + CSS + JS 인라인). 외부 빌드 불필요.
- 외부 이미지 URL은 사용하지 않는다. 그래픽은 인라인 SVG·CSS·이모지로 구현하되, 사용자 제공 K-SURE 공식 로고 PNG 1개만 base64로 단일 HTML 안에 내장하는 예외를 둔다.
- 외부 라이브러리는 최소화. 꼭 필요하면 CDN 링크로. 상태관리는 순수 JS 객체/변수로 충분.
- **브라우저 저장소(localStorage/sessionStorage) 사용 금지** — 상태는 메모리 변수로만.
- 게임 상태 객체 예시:
  ```
  state = {
    stage: 1,
    turn: 1,
    maxTurn: 4,
    fund: 10000,
    history: [],        // 각 턴 선택·결과 기록
  }
  ```
- 데이터(시장 테이블, 밸런스 표)는 코드 상단에 **설정 상수(config)로 분리**해, 나중에 실제값 교체가 쉽도록 한다.
- 랜덤 판정은 `Math.random()` 사용. 디버그용으로 "확률 고정" 토글이 있으면 시연에 편리(선택).

---

## 8. Codex 작업 순서 (권장)

1. **골격**: 단일 HTML + 화면 전환(시작→턴→결과) 구조부터.
2. **config + 데이터 로딩**: 4장 밸런스 표(RI_TABLE/CREDIT_TABLE/GRADE_TABLE)를 상수로. 5.2.2 시장 리스트 JSON을 코드에 심고, 5.2.3 랜덤 추출·파생값 계산 함수 구현.
3. **스테이지 1 완성**: 매 턴 랜덤 3개 시장 추출 → 시장 선택 → 후불 고정 판정 → 자금 반영 → 턴 반복 → 종료. **여기까지가 최소 완성본.** (초기엔 `isSampleData: true` 예시 JSON으로 개발)
4. **피드백 문구**: 각 결과에 데이터 연결 한 줄 추가.
5. **스테이지 2 확장**: 결제조건·보험 선택 및 4.3~4.4 보정, 비상위험 이벤트(4.2.2) 로직.
6. **해킹 사기 이벤트**(4.6) 추가.
7. **마감 처리**: 예시데이터 안내 문구, 면책 문구, 데이터 출처 표기 확인. 실제 JSON으로 교체 후 `isSampleData: false`.

> 3번(스테이지 1)까지만 돼도 발표 시연이 가능하다. 시간이 부족하면 3번을 확실히 완성하는 것을 최우선으로 한다.

---

## 9. 발표 전 최종 체크리스트 (주완 확인용)

- [ ] 세 파일(위험지수·신용위험지수·국가신용등급)을 받아 시장 리스트 JSON으로 합쳤는가 (종류 A)
- [ ] 세 값이 모두 존재하는 국가·업종 조합만 넣었는가 (없는 값 추측 금지)
- [ ] 신용위험지수의 낮음/중간/높음 구간 기준을 정해 문서화했는가
- [ ] `isSampleData`를 실제 데이터로 채운 뒤 false로 바꿨는가 (아니면 "예시 데이터" 문구 유지)
- [ ] 데이터 출처(한국무역보험공사 공공데이터) 표기가 있는가
- [ ] "미래 예측"으로 읽히는 표현이 없는가
- [ ] 실존 기업을 위험하다고 단정하는 표현이 없는가
- [ ] 면책 문구(교육용)가 있는가
- [ ] 모바일에서 정상 표시되는가

---

## 부록 A. 밸런스 검증 결과 (정책별 3만 회)

실제 구현과 같은 2+4턴, 매 턴 낮은 RI 후보 선택, 스테이지 1 후불·무보험
고정 조건으로 검증했다. 성공은 스테이지 2 시작 자금 대비 +400이다.

| 스테이지 2 정책 | 최종 평균 | 2단계 평균 증감 | 파산율 | 성공률 |
|---|---:|---:|---:|---:|
| 후불(전액)·무보험 | 8,945 | -689 | 7.7% | 45.3% |
| 후불(전액)·보험 | 9,843 | +200 | 0.7% | 45.9% |
| 부분선불(50%)·무보험 | 9,990 | +339 | 0.8% | 63.1% |
| 부분선불(50%)·보험 | 10,045 | +399 | 0.0% | 63.3% |
| 선불(전액)·무보험 | 9,861 | +212 | 0.5% | 68.0% |
| 선불(전액)·보험 | 9,900 | +264 | 0.0% | 64.2% |

스테이지 1 종료 시 시작 자금 이상 비율은 약 67.1%지만, 최종 성공을
스테이지 2 상대 성과로 판정하므로 이 운이 승패를 선점하지 않는다.
보험은 후불 파산율을 크게 낮추고, 선불은 손실 노출과 보험비용이 모두 남아
무손실 조합이 아니다. 상세 조건과 재현 명령은 `balance-report.md`와
`scripts/simulate_balance.py`에 둔다.

---

## 부록 B. Codex 지시문 (복붙용)

### B-1. 데이터 조인 → JSON 생성 지시문

> 아래 세 CSV 파일을 국가+업종 기준으로 조인해서 하나의 JSON 배열로 만들어줘.
>
> - 파일1: 한국무역보험공사_국가별 업종별 위험지수 (RI등급)
> - 파일2: 한국무역보험공사_주요 업종별 신용위험지수 (R급 비율)
> - 파일3: 한국무역보험공사_국가신용등급 (1~7)
>
> 요구사항:
> 1. 세 값이 **모두 존재하는** 국가·업종 조합만 포함할 것. 없는 값은 절대 추측·보간하지 말고 그 행을 버릴 것.
> 2. 국가·업종 명칭 표기가 파일마다 다를 수 있으니(예: "베트남"/"Vietnam", "자동차부품"/"자동차 부품") 표기를 통일해서 조인할 것.
> 3. 신용위험지수(연속 숫자)는 **3분위** 기준으로 낮음/중간/높음 3구간으로 변환할 것. 사용한 구간 경계값을 함께 출력할 것.
> 4. 국가는 ISO 3166 alpha-3 코드도 붙일 것.
> 5. RI 기준 세 층(RI1~2 / RI3 / RI4~5)이 **각각 최소 2~3개 이상** 포함되도록 결과를 확인하고, 부족하면 알려줄 것.
> 6. 출력 스키마:
>
> ```json
> { "source": "...", "isSampleData": false,
>   "markets": [
>     {"id":"M001","countryCode":"VNM","country":"베트남","sector":"가구","ri":2,"creditRisk":"낮음","countryGrade":3}
>   ]}
> ```
>
> 파생값(사고확률·수익률 등)은 JSON에 넣지 말 것. 게임 코드가 런타임에 계산한다.

### B-2. 게임 개발 지시문

> 첨부한 개발 명세서(trade-sim-dev-spec.md)를 그대로 구현해줘. 핵심 조건:
>
> - **단일 HTML 파일** (HTML+CSS+JS 인라인). 외부 이미지 URL 금지. 그래픽은 인라인 SVG·CSS·이모지를 사용하되, 사용자 제공 K-SURE 공식 로고 PNG 1개만 base64 내장을 허용.
> - **localStorage/sessionStorage 사용 금지.** 상태는 메모리 변수로만.
> - 4장의 밸런스 표(RI_TABLE / CREDIT_TABLE / GRADE_TABLE / 결제조건 / 보험)는 **코드 상단 config 상수**로 분리할 것. 수치를 바꾸기 쉬워야 함.
> - 5.2.2 시장 리스트 JSON을 코드에 심고, 5.2.3 **층화 추출**(RI 층별 1개씩 총 3개)로 매 턴 후보를 뽑을 것. 카드 노출 순서는 섞을 것.
> - 판정은 4.5 의사코드 그대로: **비상위험 이벤트 먼저 판정 → 없으면 일반 사고 판정**.
> - 2장 절대 원칙 준수: "미래 예측" 표현 금지, 실존 기업을 위험하다고 단정 금지, 데이터 출처·면책 문구 표기.
> - 8장 작업 순서대로 진행하되, **3번(스테이지 1)까지를 최우선**으로 완성할 것. 여기까지만 돼도 시연 가능해야 함.
>
> 완성 후 각 화면 스크린샷 대신 실행 방법을 알려줘.
